# Roadmap

## v0.1.1

### 计划功能

- 增加主机分组与标签筛选，支持按组执行命令
- 增加常用运维任务预设，减少重复输入
- 优化 `status` 与 `check` 的输出样式，提升可读性
- 容器列表支持运行中 / 已停止分组显示
- 增加可配置 SSH 模板与超时预设
- 支持巡检结果导出为文本报告

## 后续储备

- 提供原生 PowerShell 实现，减少对 Bash 的依赖
- 增加可选 Web 总览面板
- 设计插件式命令扩展机制
- 支持可选的加密配置存储
